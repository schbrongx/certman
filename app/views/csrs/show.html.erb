<p id="notice"><%= notice %></p>

<p>
  <strong>Name:</strong>
  <%= @csr.name %>
</p>

<p>
  <strong>Content:</strong>
  <%= simple_format @csr.content.html_safe %>
</p>

<%= link_to 'Edit', edit_csr_path(@csr), class:"btn btn-default" %> 
<%= link_to 'Back', csrs_path, class:"btn btn-default" %>
