<%# render view as partial, so it's accessable for bootstrap modals %>
<%= render 'show' %>
<%= link_to 'Edit', edit_keypair_path(@keypair), class:"btn btn-xs btn-default" %>
<%= link_to 'Back', request.referer, class:"btn btn-xs btn-default" %>
